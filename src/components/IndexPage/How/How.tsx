import { type FC } from 'react';

import s from './How.module.scss';

export interface Props {
    //
}

export const How: FC<Props> = ({ }) => {
    return (
        <div className={s.root}>
        </div>
    );
};
